# encoding: utf-8

import re

# print(re.search('.','hello123wolrd!'))
#匹配包含。不要求从字符串的第一个字符就匹配。只要字符串当中有匹配该规则的，则就匹配成功。
# print(re.match('..............','hello123wolrd!\n'))
#从头开始匹配。从字符串的第一个字符开始匹配，如果第一个字符不匹配规则，那么匹配失败。
#re.findall(re规则，字符串)：把所有匹配的字符放在列表中并返回。
#re.sub(re规则，替换串，被替换串)：匹配字符并替换。


print(re.search('ne/B','laraine!'))





